# Generated manually to sync database state
from django.db import migrations, models
import django.db.models.deletion
import django.utils.timezone


class Migration(migrations.Migration):

    dependencies = [
        ('core_hr', '0003_alter_company_code'),
    ]

    operations = [
        # This migration is a no-op since the database is already in the correct state
        # We're just marking the migration as applied to sync Django's migration state
    ]
